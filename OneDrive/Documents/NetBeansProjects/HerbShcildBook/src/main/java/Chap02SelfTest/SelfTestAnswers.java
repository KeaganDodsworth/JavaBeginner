/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Chap02SelfTest;

/**
 *
 * @author keaga
 */
public class SelfTestAnswers {
    /*
    
    1.to ensure portability across platforms.
    
    2.The char data type in Java is a single 16-bit Unicode character. 
    It represents a character that could be any one of the world languages. 
    Other programming languages support 256 characters in general, 
    and most of them represent ASCII / EBCDIC characters. 
    They do not support world language characters.
    
    3.False
    
    4.System.out.println("One \n" + "Two \n" + "Three");
    
    5.the variable sum in instantiated inside the code block but then called for outside the code block
    
    6.Postfix increment operator means the expression is evaluated first using the original value of the variable and then the variable is incremented
    Prefix increment operator means the variable is incremented first and then the expression is evaluated using the new value of the variable
    
    7.if(d != 0 && (n % d) == 0)
    
    8.int
    
    9.when converting a smaller type to a larger type size.
    
    10.Check for file PrimeNum.java
    
    11.no
    
    12.Yes
    
    */
}
